import { Box } from "@mui/material";

const CustomDivider = () => {
  return (
    <Box
      sx={{
        height: "1px",
        background: "#ddd",
        ml: 2,
        width: "100%",
      }}
    />
  );
};

export default CustomDivider;
